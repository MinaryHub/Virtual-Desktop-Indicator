# Build output
bin/
obj/
publish-sc/

# Installer is distributed via GitHub Releases, not committed to the repo
installer/

# Debug / local
*.user
debug.log

# Per-machine auto-incrementing build counter (see StampBuildVersion target)
build.counter

# OS
Thumbs.db
Desktop.ini
